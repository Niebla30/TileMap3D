extends Node2D

var pos_grid: Vector2i # i,j en la grid
var pos_heightmap: Vector2i #pixel del heightmap que representa
var height: float #altura que coge del heightmap
var id: int #indice de la matriz


# Called when the node enters the scene tree for the first time.
func _ready() -> void:
	pass # Replace with function body.


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass
	
